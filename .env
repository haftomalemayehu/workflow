# Default docker-compose profile. Plain `docker compose up` reads this and starts the SQLite
# `app` service. `docker compose --profile postgres up` overrides it (CLI --profile replaces
# COMPOSE_PROFILES rather than adding to it) and starts `postgres` + `app-postgres` instead.
COMPOSE_PROFILES=sqlite
